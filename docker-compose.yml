services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: js-dev
    ports:
      - "80:80"
    volumes:
      - ./app:/usr/share/nginx/html               # Mapuje lokalny kod do kontenera